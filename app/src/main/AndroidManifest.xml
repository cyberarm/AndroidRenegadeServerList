<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidRenegadeServerList">
        <service
            android:name="dev.cyberarm.android_renegade_server_list.library.RenegadeServerListService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="dev.cyberarm.android_renegade_server_list.ServerSettingsActivity" />
        <activity android:name="dev.cyberarm.android_renegade_server_list.AppSettingsActivity" />
        <activity android:name="dev.cyberarm.android_renegade_server_list.ServerViewActivity" />
        <activity android:name="dev.cyberarm.android_renegade_server_list.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="dev.cyberarm.android_renegade_server_list.library.AppOnBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>